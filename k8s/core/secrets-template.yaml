apiVersion: v1
kind: Secret
metadata:
  name: agency-secrets
  namespace: agency
type: Opaque
stringData:
  # SUPABASE CREDENTIALS (CLOUD)
  SUPABASE_URL: "https://your-project.supabase.co"
  SUPABASE_KEY: "your-service-role-key"

  # LLM PROVIDER KEYS
  OPENAI_API_KEY: "sk-..."
  ANTHROPIC_API_KEY: "sk-ant-..."
  RUNPOD_API_KEY: "rpa-..."
  HETZNER_API_TOKEN: "..."

  # AGENCY SETTINGS
  AGENCY_NAME: "Autonomous Dev Agency"
